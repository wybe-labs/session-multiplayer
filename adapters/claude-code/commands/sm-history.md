---
description: Show recent chat history of a multiplayer room (Session Multiplayer)
---

Use the session-multiplayer MCP server's show_history tool with room_name "$ARGUMENTS" (if no room was given and I'm only in one room, use that one; otherwise ask which). Show me the messages in order with who sent them and from which machine/session. This is read-only — it doesn't consume my unread inbox.
